export const invalidateCache = ({
    product,
    order,
    admin
})=>{
    if(product){

        const productKeys = [
            "latest-prodcuts",
            "categories",
            "all-products"
        ]
    }
}

